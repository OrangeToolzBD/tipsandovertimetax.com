# tipsandovertimetax.com

Static calculator site. Source files are at the repo root; `build/` holds the build spec and
keyword research.

## Status: WORK IN PROGRESS — not ready to publish

This commit is a checkpoint, not a launch. Outstanding before go-live:

- `WRITE:` placeholders still present in page copy
- `REPLACE_AUTHOR_NAME`, `REPLACE_CONTACT_EMAIL`, `REPLACE_GA4_ID` not yet filled in
- `node tools/check-site.mjs` does not yet print READY TO LAUNCH

**Do not enable GitHub Pages until the above are resolved** — placeholder text would be publicly visible.
