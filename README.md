# tipsandovertimetax.com

Static calculator site. Site files are at the repo root; `build/` holds the build spec and
keyword research.

## Status: WORK IN PROGRESS — not ready to publish

Pages with unresolved `WRITE:` or `REPLACE_` placeholders: **8**

Before enabling GitHub Pages, `node tools/check-site.mjs` must print READY TO LAUNCH.
Publishing earlier would expose placeholder text on a live domain.
